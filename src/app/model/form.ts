import { EmailValidator, MaxLengthValidator } from '@angular/forms';
import { logging } from 'protractor';

export class Form {
    Application_Name: string;
    passport_number: number;
    Country_of_nationality: string;
    gender: string;
    duration_of_visa: Date;
    SERVICE_OPTION: String;
    CONTACT_EMAIL: EmailValidator;





}
